# Requirements for MTG Draft Coach POC

# OpenAI API
openai>=1.0.0

# HTTP requests for Scryfall API
requests>=2.31.0

# Progress bars (optional but recommended for large files)
tqdm>=4.66.0

# Parquet support (recommended for large files)
pandas>=2.0.0
pyarrow>=14.0.0

# Pretty printing (optional but recommended)
rich>=13.0.0

# Web scraping for knowledge base
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Vector database for RAG
chromadb>=0.4.0

# Local embeddings (optional, alternative to OpenAI)
sentence-transformers>=2.2.0

# Search engine integration for discovery agent
duckduckgo-search>=4.0.0

# Note: No additional dependencies needed for:
# - Draft data processing (uses standard library: csv, sqlite3)
# - Log reading (uses standard library)

